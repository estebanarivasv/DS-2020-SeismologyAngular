export class SensorsDynamicModel {

    // FILTERS
    name: string;
    status: boolean;
    active: boolean;
    user_id: number;

    // PAGINATION
    page?: number;
    elem_per_page?: number;

    // SORTING
    sort_by?: string;
    direction?: string;
}


